window.CAHOOTS_CONFIG = {
  API_URL: '/api',  // Use nginx proxy for all environments
  WS_URL: '/ws',    // Use nginx proxy for all environments
  ENVIRONMENT: 'development'  // Set to 'development' for local, 'production' for deployed
};
